# .github/workflows/deploy-dev.yaml
name: Polybot Deploy Dev

on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string

  workflow_dispatch:
    inputs:
      image:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug Secrets
        run: |
          echo "✅ EC2_USERNAME=${{ secrets.EC2_USERNAME }}"
          echo "✅ EC2_HOST=${{ secrets.EC2_HOST }}"

      - name: Check if EC2 secrets exist
        run: |
          if [ -z "${{ secrets.EC2_USERNAME }}" ]; then echo "❌ EC2_USERNAME is missing!"; else echo "✅ EC2_USERNAME is set."; fi
          if [ -z "${{ secrets.EC2_HOST }}" ]; then echo "❌ EC2_HOST is missing!"; else echo "✅ EC2_HOST is set."; fi
      - name: Echo secrets with redaction
        run: |
          echo "EC2_USERNAME: ***${{ secrets.EC2_USERNAME != '' }}"  # should print true
          echo "EC2_HOST: ***${{ secrets.EC2_HOST != '' }}"          # should print true

      - name: SSH and Deploy
        env:
          PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          EC2_USER: ubuntu
          EC2_HOST: 54.183.125.83
          IMAGE: ${{ inputs.image }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          echo "$PRIVATE_KEY" > key.pem
          chmod 400 key.pem

          echo "🌐 SSH to $EC2_USER@$EC2_HOST"

          ssh -o StrictHostKeyChecking=no -i key.pem $EC2_USER@$EC2_HOST << EOF
            cd PolybotService

            echo "IMG_NAME=$IMAGE" > .env
            echo "TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN" >> .env
            echo "TELEGRAM_APP_URL=https://proxydeema.fursa.click" >> .env

            docker compose -f docker-compose.dev.yaml down || true
            docker compose -f docker-compose.dev.yaml pull
            docker compose -f docker-compose.dev.yaml up -d --build
          EOF
